# fhir-seam environment variables
# No environment variables required for Phase 1–5 (mock endpoint needs none).
# Real EHR integration (Phase 7+) would require OAuth credentials here.
# VITE_EHR_API_URL=https://your-ehr-fhir-endpoint.example.com
